# TEMPLATE ONLY — review addresses, ports and Docker networking before applying.
# Example principle for a final Linux deployment:
#   - allow loopback
#   - allow established/related traffic
#   - allow only explicitly required internal service ports
#   - reject unapproved outbound traffic
# Never apply this template without adapting it to the deployment topology.
