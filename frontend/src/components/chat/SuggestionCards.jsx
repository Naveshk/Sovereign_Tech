import React from 'react';import {Code2,FileText,Image,Lightbulb} from 'lucide-react';const cards=[['Explain this concept','Explain a technical topic clearly with examples.',Lightbulb],['Analyze an image','Inspect an uploaded image or diagram.',Image],['Write code','Generate, debug or explain code locally.',Code2],['Generate a report','Create a professional Word document.',FileText]];export default function SuggestionCards({onSelect}){return <div className="suggestions">{cards.map(([t,d,I])=><button key={t} onClick={()=>onSelect(d)}><I size={18}/><b>{t}</b><span>{d}</span></button>)}</div>}
