import React,{useRef} from 'react';import {Paperclip,Send,X} from 'lucide-react';import {ACCEPT,formatSize} from '../../utils/fileUtils';export default function ChatInput({value,onChange,onSend,file,setFile,disabled}){const ref=useRef();return <div className="input-wrap">{file&&<div className="file-chip">📎 {file.name}<small>{formatSize(file.size)}</small><button onClick={()=>setFile(null)}><X size={14}/></button></div>}<div className="input-row"><button className="attach" onClick={()=>ref.current?.click()} disabled={disabled}><Paperclip size={20}/></button><input ref={ref} type="file" accept={ACCEPT} hidden onChange={e=>setFile(e.target.files?.[0]||null)}/><textarea value={value} disabled={disabled} onChange={e=>onChange(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();onSend()}}} placeholder="Ask Anything..." rows="1"/><button className="send" onClick={onSend} disabled={disabled||(!value.trim()&&!file)}><Send size={19}/></button></div></div>}
